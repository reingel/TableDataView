# TableDataView

## 개요
- 본 프로그램은 Visual Studio Code Extension이다.
- csv, tsv 등 다양한 이블 형태의 텍스트 파일을 테이블 형식으로 보여준다.
- 추후 vsix로 배포한다.


## 기능
### 파일 읽기
- .csv, .txt, .dat 등 다양한 형태의 아스키 파일을 읽을 수 있다.
- 컴마, 탭, 공백 등 다양한 delimiter를 자동 인식하여 행렬형태로 인식한다.
- 헤더가 포함되어 있는 경우 헤더를 읽어 각 컬럼데이터의 이름으로 사용한다.

### 테이블 보기
- 사용자가 vscode에서 해당 파일을 선택한 후 오른쪽 마우스를 눌러 'TableDataView: table view'를 선택하면 헤더와 데이터를 테이블 형태로 표현한다.
- 각 컬럼은 좌, 중, 또는 우 정렬이 가능하다.
- 마우스로 각 컬럼을 선택할 수 있다. (다중선택 가)

### 그래프 보기
- 컬럼을 선택한 후 오른쪽 마우스를 눌러 'Show graph'를 선택하면 그래프를 표시한다.



